rootProject.name = "kafkaproxy"

include rootDir.listFiles().findAll {
    it.isDirectory() && !['.','..'].contains(it) && file("$it.absolutePath/build.gradle").exists()
}.collect {
    it.getName()
}.toArray(new java.lang.String[0])